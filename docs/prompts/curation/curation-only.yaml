# Partial curation config — curation skill only (for testing)
phase: skill-curation-test
merge_target: curation/test

stages:
  - name: "Curate curation skill"
    groups:
      - id: curation
        branch: curation/curation
        merge_message: "docs: curate curation skill"
