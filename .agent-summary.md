# Agent Summary — Issue #62

## What Changed and Why

**`src/sheets/sheetsSync.ts`** (the bug fix):
- Imported `SHEET_COLUMNS` from `./sheetsMapper`
- Added `export function columnLetter(n: number): string` helper that converts a 1-based column index to a spreadsheet column letter (A, B, ..., Z, AA, AB, ...)
- Changed line 66 from hardcoded `A1:R${rows.length}` to `A1:${columnLetter(SHEET_COLUMNS.length)}${rows.length}` — currently resolves to `A1:T{N}` for 20 columns, and will automatically expand if new columns are added to `SHEET_COLUMNS`

**`src/sheets/__tests__/sheetsSync.test.ts`** (new test file):
- Tests for `columnLetter` helper: A(1), R(18), T(20), Z(26), AA(27)
- Tests for `scheduleSave` write range: correct end column (T), not hardcoded R, correct row count (tasks + header)

## Tests Added

New file: `src/sheets/__tests__/sheetsSync.test.ts`
- 5 tests for `columnLetter` helper correctness
- 3 tests for `scheduleSave` write range construction

All existing tests in `sheetsMapper.test.ts` already covered Tests 2 (row width = SHEET_COLUMNS.length) and Test 3 (constraint field round-trip) from the issue requirements.

## What Couldn't Be Done

Nothing — this was a straightforward fix. Full unit test suite (272 tests) passes, TypeScript check passes.

## Verification Output

```
Test Files  15 passed (15)
      Tests  272 passed (272)
   Duration  6.11s
```

TypeScript: no errors (`npx tsc --noEmit` exits 0)

Note: `./scripts/full-verify.sh` requires Docker (for WASM build and E2E relay). Unit tests and tsc were run directly and pass.
