# Environment & secrets
.env
.env.local
.env.*.local

# Local configurations
settings.local*

# Dependencies
node_modules/

# Build output
dist/
build/
.vite/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.idea/

# Claude (track settings.json and worktree context, ignore everything else)
.claude/*
!.claude/settings.json
!.claude/worktrees/
.claude/worktrees/*
!.claude/worktrees/CLAUDE.md

# Agent ephemeral state (lives in worktrees, cleaned up with them)
claude-progress.txt
.agent-summary.md

# Docker
docker-compose.override.yml

# WASM build output
src/wasm/
**/target/

# Google Sheets credentials (NEVER commit these)
credentials.json
token.json
*.credentials.json
tsconfig.tsbuildinfo

# Logs & test artifacts
logs/
test-results/
