// Local dev stub — overridden by Go server in cloud deployments.
// Runtime config values come from Cloud Run env vars in deployed environments.
// For local development, use .env files with VITE_* prefixed variables.
window.__ganttlet_config = {};
