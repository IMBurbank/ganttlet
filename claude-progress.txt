A1: DONE — read current CLAUDE.md (210 lines), mapped content to destinations
A2: DONE — created docs/architecture.md and docs/multi-agent-guide.md
A3: DONE — created 8 skill files in .claude/skills/ with YAML frontmatter
A4: DONE — rewrote CLAUDE.md to 112-line lean core
A5: DONE — verified all content mapped, all skills have frontmatter, line count in range
A6: DONE — all changes committed, no files outside scope modified
B1: DONE — structured error extraction in validation retry (both WATCH and non-WATCH)
B2: DONE — --max-turns and --max-budget-usd on all claude invocations
B3: DONE — conflict diffs and branch summary injected into merge conflict prompt
B4: DONE — partial stage success with succeeded/failed tracking, merge skips failed groups
B5: DONE — preflight_check() verifies git state, prompt files, and WASM build
B6: DONE — MODEL env var support with --model flag
B7: DONE — monitor_agent() watchdog with STALL_TIMEOUT config
B8: DONE — final verification passed (bash -n OK, shellcheck clean, all committed)
C1: DONE — fixed exit code capture bug (PIPESTATUS), added AGENT_SCOPE env var to verify.sh
C2: DONE — added output deduplication via md5sum hash comparison
C3: DONE — added 30s rate limiting (configurable via VERIFY_COOLDOWN)
C4: DONE — compact output: error-count-first for tsc, summary for vitest
C5: DONE — created pre-commit-hook.sh rejecting todo!(), unimplemented!(), commented-out tests
C6: DONE — verified .claude/settings.local.json is valid JSON with correct hook config
C7: DONE — all syntax checks pass, all changes committed
D1: DONE — created issue template with all required fields (summary, acceptance-criteria, scope, files, complexity, context)
D2: DONE — created quality gate workflow with 4 validation checks
D3: DONE — overhauled agent-work.yml with rich prompts, retry loop, complexity config, agent summary PR body
D4: DONE — all YAML files validated, only target files modified
D5: DONE — all success criteria verified

=== Plugin Adoption Plan ===
Runbook: docs/plugin-adoption-plan.md
Status: WAITING for user manual steps (M1: OAuth token, M2: GitHub secret)
Next: After user confirms "manual steps done", execute A1-A6 from the runbook.
A1: DONE — Created .claude/settings.json (4 plugins + 2 PreToolUse hooks). Fixed .gitignore negation.
A2: DONE — Updated Dockerfile (rust-analyzer via rustup, typescript-language-server via npm global)
A3: DONE — Updated agent-work.yml (ANTHROPIC_API_KEY→CLAUDE_CODE_OAUTH_TOKEN, plugin install+cache, code-review gated >50 lines)
A4: DONE — All 6 hook tests pass (blocks wasm/env/lockfile/push-main, allows normal/feature-branch)
A5: DONE — npm run build passes (429 modules, 2.35s)
A6: DONE — Committed on feature/plugin-adoption branch (2c12b1b)
Status: WAITING for user manual steps (M1: extract OAuth token from Docker, M2: add GitHub secret) and verification (V1-V4)
